POSTGRES_DB=aulos
POSTGRES_USER=aulos
POSTGRES_PASSWORD=replace-with-a-long-random-value
ADMIN_TOKEN=replace-with-a-different-long-random-value
POLL_INTERVAL_SECONDS=300
REQUEST_TIMEOUT_SECONDS=20
MAX_ITEMS_PER_FEED=30
CORS_ORIGINS=https://aulos-news.ian-g-lacey2.chatgpt.site

# Optional: server-side only. Never expose these in browser code or commit real values.
ALPACA_API_KEY_ID=
ALPACA_API_SECRET_KEY=
ALPACA_DATA_BASE_URL=https://data.alpaca.markets

# Optional free key. OpenAlex works without one at a much lower daily allowance.
OPENALEX_API_KEY=

# Optional free official energy-data key for the next EIA connector.
EIA_API_KEY=

# Hosted frontend runtime value (configured in Sites, not required by Docker):
AULOS_BACKEND_URL=http://62.238.119.174
